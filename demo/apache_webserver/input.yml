---
name: Apache web server
description: Install apache web server on an individual node

provision:
  - name: webserver
    host: 10.8.245.88
    ansible_vars:
      ansible_user: fedora
      ansible_ssh_private_key_file: /home/rywillia/.ssh/pit-jenkins

orchestrate:
  - name: playbooks/system-release
    description: Setup basic apache webserver
    hosts: webserver
    scm:
      url: https://github.com/rywillia/awx-test.git